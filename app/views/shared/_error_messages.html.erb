<% if object.errors.any? %>
  <div class="error_explanation">
    <h2>
      <%= object.errors.count %>つのエラーが確認されました。
    </h2>
    <ul>
  <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
