<ul class="user--works__types">
  <%= form_tag(url_for(action: 'my_works'), remote: true) do %>
    <span><%= submit_tag "作品一覧" %></span>
  <% end %>
  <%= form_tag(url_for(action: 'favorites'), remote: true) do %>
    <span class="user--works__now"><strong><%= submit_tag "お気に入り" %></strong></span>
  <% end %>
</ul>
<% if @user.likes.present? %>
  <ol class="works">
    <% @user.likes.each do |like| %>
      <%= render like.work %>
    <% end %>
  </ol>
<% else %>
  <h4>いいねした作品がありません</h4>
<% end %>
