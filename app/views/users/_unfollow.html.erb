<%= form_with(model: current_user.active_relationships.find_by(followed_id: @user.id),
              html: {method: :delete}, remote: true, data: { confirm: "フォローを解除しますか？" }) do |f| %>
  <%= f.submit "フォロー中", class: "btn follow--btn" %>
<% end %>